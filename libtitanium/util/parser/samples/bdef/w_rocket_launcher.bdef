Weapon w_rocket_launcher
{
    // Core definitions, these probably won't be changing ever
    locale
    {
        name = "#WEAPON_w_rocket_launcher"
        description = "#WEAPON_DESC_w_rocket_launcher"
    }

    script
    {
        file = "script/weapon/w_rocket_launcher.as"
        callback_Fire = "OnFire"
    }

    modDir = "./weapon_mods/w_rocket_launcher"

    // actual weapon stats! should be changing often
    fireRate = 55

    reloads = true
    ammo = 4
    clips = 5
}

#pragma once

#include <concepts>

#include "titanium/util/numerics.hpp"

namespace util::data
{
    template <typename TKey, typename KValue, std::unsigned_integral TSize = u32>
    class Map
    {
        
    };
}

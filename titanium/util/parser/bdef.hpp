#pragma once

#include <stdlib.h>
#include "titanium/util/data/datanode.hpp"

/*

Functions for parsing the bdef format, a simple readonly key/value object format

*/
namespace util::parser::bdef
{
    //utils::data::DataNode_s Parse(const char *const pszInputString);
    //utils::data::DataNode_s Parse_n(const char *const pszInputString, const size_t nBufSize);

    //char* Unparse(const utils::data::DataNode_s kvNode);
    //void Unparse_n(const utils::data::DataNode_s kvNode, char *const poszBuf);
};